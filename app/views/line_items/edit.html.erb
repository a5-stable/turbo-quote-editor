<main class="container">
  <%= link_to sanitize("&larr; Back to quote"), quote_path(@quote) %>

  <div class="header">
  </div>

  <%= turbo_frame_tag nested_dom_id(@line_item) do %>
    <%= render "form",
              quote: @quote,
              line_item_date: @line_item_date,
              line_item: @line_item %>
  <% end %>
</main>
