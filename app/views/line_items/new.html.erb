<main class="container">
  <div class="header">
    <h1>New LineItem</h1>
  </div>

  <%= turbo_frame_tag LineItem.new do %>
    <%= render "form", quote: @quote, line_item_date: @line_item_date, line_item: @line_item %>
  <% end %>
</main>
