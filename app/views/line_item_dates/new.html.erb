<main class="container">
  <div class="header">
    <h1>New quote</h1>
  </div>

  <%= turbo_frame_tag LineItemDate.new do %>
    <%= render "form", line_item_date: @line_item_date, quote: @quote %>
  <% end %>
</main>
